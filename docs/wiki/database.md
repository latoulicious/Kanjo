# Database

Postgres is the system of record. Migrations are **owned by the API**, written
as [goose](https://github.com/pressly/goose) SQL files in `api/migrations/`, run
at API boot (or via the goose CLI). Query code is generated by
[sqlc](https://sqlc.dev) from those migrations + `api/internal/store/queries/`.

## Tooling

- **goose** — schema migrations. `goose -dir api/migrations create <name> sql`.
- **sqlc** — typed Go from SQL. Config in `api/sqlc.yaml`; schema source is the
  migrations dir, queries in `internal/store/queries/`. Run `sqlc generate`
  after editing either.

Neither tool is required to boot the HTTP server; both are required before the
store layer does real queries. Install: `go install github.com/pressly/goose/v3/cmd/goose@latest`
and `go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest` (or use Docker images).

## Schema (planned)

Core tables follow the domain (`domain.md`): `accounts`, `categories`,
`transactions`, `projects`. Balances, burn, runway, and reports are **derived**
from `transactions` — never stored as incrementing counters.

> Placeholder — no migrations exist yet. Populate with schema detail (amount
> representation, currency, sign convention, indexes) as the first migration
> lands. Code is the source of truth.
