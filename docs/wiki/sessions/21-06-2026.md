---
time: 04:37 PM
type: feature
breaking_change: false
modules:
  - docs
---

# Summary

Scaffolded the **docs-first skeleton** for Kanjo and extracted the plan from
`Kanjo.pdf` into `docs/wiki/`. The `docs/wiki/` layout mirrors the Atelier
Lazyscan wiki (`/Atelier/Lazyscan/docs/wiki`) as the skeleton reference; content
is Kanjo-specific. Added a root `AGENTS.md`.

Code skeleton (`api/` + `web/`) was started during the session then **reverted at
the user's request** — implementation begins in a later session. This pass is
docs only.

# Files Touched

- `AGENTS.md` (new) — agent governance: role, planned layout, wiki index, session
  logging, change-safety, commit convention.
- `docs/wiki/` (new) — README, principles, design-system, architecture, domain,
  conventions, database, known-constraints, troubleshooting, findings ↔
  resolutions (paired), nice-to-have, approved-to-implement, deferred-notes,
  deploy; `modules/ sessions/ decisions/` with `.gitkeep`.
- Reverted (deleted before commit): `api/` Go walking-skeleton (go.mod, config,
  store/pgxpool, net/http router + health) and empty `web/` tree.

# New Behavior

Project has a documented spine: philosophy, design system (palette/typography/
components/IA from the plan), target architecture (SPA → nginx → Go API →
Postgres), domain model, and conventions — before any code is written.

# Reason For Change

User request: scaffold the project skeleton and extract plan/principle/design
from the plan PDF into `docs/wiki`, using Lazyscan's wiki as the skeleton ref.

# Decisions

- **Stack deviations from the PDF** (user, this session): backend routing is
  stdlib `net/http` (Chi dropped); edge is **nginx** (Caddy dropped), matching
  Lazyscan.
- **Ports**: web host bind **8090**, API **3000**. 8080 avoided — Dozzle owns it
  on the VPS.
- **Code deferred**: walking skeleton reverted; docs only this pass.

# Risks

- Several docs are forward-looking (reference `api/`, `web/`, `migrations/`,
  `sqlc.yaml`, `tailwind.config.ts`) that do not exist yet — written as intent,
  not as claims of existing code. Reconcile when the code lands; code is the
  source of truth.

# Notes

- Not committed; awaiting human gate.
- No code review run — no code changed (docs only).
