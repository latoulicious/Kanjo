---
time: 04:37 PM
type: feature
breaking_change: false
modules:
  - docs
---

# Summary

Scaffolded the **docs-first skeleton** for Kanjo and extracted the plan from
`Kanjo.pdf` into `docs/wiki/`. The `docs/wiki/` layout mirrors the Atelier
Lazyscan wiki (`/Atelier/Lazyscan/docs/wiki`) as the skeleton reference; content
is Kanjo-specific. Added a root `AGENTS.md`.

Code skeleton (`api/` + `web/`) was started during the session then **reverted at
the user's request** — implementation begins in a later session. This pass is
docs only.

# Files Touched

- `AGENTS.md` (new) — agent governance: role, planned layout, wiki index, session
  logging, change-safety, commit convention.
- `docs/wiki/` (new) — README, principles, design-system, architecture, domain,
  conventions, database, known-constraints, troubleshooting, findings ↔
  resolutions (paired), nice-to-have, approved-to-implement, deferred-notes,
  deploy; `modules/ sessions/ decisions/` with `.gitkeep`.
- Reverted (deleted before commit): `api/` Go walking-skeleton (go.mod, config,
  store/pgxpool, net/http router + health) and empty `web/` tree.

# New Behavior

Project has a documented spine: philosophy, design system (palette/typography/
components/IA from the plan), target architecture (SPA → nginx → Go API →
Postgres), domain model, and conventions — before any code is written.

# Reason For Change

User request: scaffold the project skeleton and extract plan/principle/design
from the plan PDF into `docs/wiki`, using Lazyscan's wiki as the skeleton ref.

# Decisions

- **Stack deviations from the PDF** (user, this session): backend routing is
  stdlib `net/http` (Chi dropped); edge is **nginx** (Caddy dropped), matching
  Lazyscan.
- **Ports**: web host bind **8090**, API **3000**. 8080 avoided — Dozzle owns it
  on the VPS.
- **Code deferred**: walking skeleton reverted; docs only this pass.

# Risks

- Several docs are forward-looking (reference `api/`, `web/`, `migrations/`,
  `sqlc.yaml`, `tailwind.config.ts`) that do not exist yet — written as intent,
  not as claims of existing code. Reconcile when the code lands; code is the
  source of truth.

# Notes

- Not committed; awaiting human gate.
- No code review run — no code changed (docs only).

---
time: 08:25 PM
type: feature
breaking_change: false
modules:
  - api
---

# Summary

P1 — code skeleton. Implemented the `api/` Go walking skeleton (stdlib
`net/http`): compiles, boots **without a live DB** (lazy `pgxpool`), serves
`GET /health` reporting DB reachability, graceful shutdown. Mirrors the Atelier
sibling idiom (image-svc/mail-svc): `run() error`, `config.Load(os.Getenv)`,
`slog`, `signal.NotifyContext`, `http.Server` + `Shutdown`. Scope confirmed with
the user as **api-only** (no web/compose/migrations content).

# Files Touched

- `api/go.mod`, `api/go.sum` (new) — module `github.com/latoulicious/kanjo/api`,
  go 1.26; only dep is `pgx/v5`.
- `api/cmd/api/main.go` (new) — `main()`→`run()`: config → store → mux →
  `http.Server` → graceful shutdown (`shutdownTimeout` 10s).
- `api/internal/config/config.go` (new) — `Load(os.Getenv)`: `PORT` (default
  3000), `DATABASE_URL` (required).
- `api/internal/store/store.go` (new) — lazy `pgxpool` (no boot ping), `Ping()`
  for health, `Close()`.
- `api/internal/server/router.go` (new) — `ServeMux`: `GET /health` (200 up /
  503 down); `/api/v1` attach point (no routes yet).
- `api/sqlc.yaml`, `api/migrations/.gitkeep`, `api/internal/store/queries/.gitkeep`
  (new) — inert until the first migration.
- `docs/wiki/findings.md`, `resolutions.md` — F-001/F-002 ↔ R-001/R-002.
- `docs/wiki/decisions/0001-postgres-shared-instance.md` (new) — ADR.
- `docs/wiki/deploy.md` — drift banner → ADR-0001.

# Previous Behavior

No code — docs-only scaffold. `api/` did not exist.

# New Behavior

`api/` boots and serves `GET /health` (200 `{"db":"up"}` / 503
`{"db":"degraded"... "down"}`), survives an unreachable DB at boot, and shuts
down gracefully on SIGINT/SIGTERM. `/api/v1` mux is wired but carries no routes.

# Reason For Change

Approved P1 task: stand up the api walking skeleton (the skeleton reverted last
session), as the base for migrations + handlers in later phases.

# Decisions

- **CodeRabbit review** (`--agent --base main`): 2 low findings.
  - **F-001** PORT not range-checked → **wontfix** (R-001): an unbindable port
    already fails fast at `ListenAndServe`; matches sibling convention.
  - **F-002** missing `Read`/`WriteTimeout` → **fixed** (R-002): added
    `ReadTimeout`/`WriteTimeout` 30s.
- **ADR-0001 — shared Postgres**: Kanjo targets the external `shared-postgres`
  instance (network `shared-db`), dedicated db+role, **no bundled postgres
  container** → 2-container deploy. Confirmed against Lazyscan. `deploy.md`'s
  3-container model is superseded (drift banner added; full rewrite deferred to
  the deploy phase).

# Risks

- Skeleton only: no migrations/sqlc queries; `sqlc.yaml` + `queries/` are inert.
- `/health` returns 503 when the DB is down — a future compose healthcheck must
  read that as not-ready, not as a crash.
- `deploy.md` is now partially stale (pointed at ADR-0001 until rewritten).

# Notes

- Verified: `gofmt`/`go vet`/`go build` clean; boot smoke vs unreachable DB →
  server up + `/health` 503; SIGTERM → graceful shutdown (exit 0).
- Constraints honored: no package godoc (per user); comments ≤2 lines.
- CodeRabbit not re-run — F-002 verified by build/vet; F-001 is a decided
  wontfix that a re-run would only re-surface.
- Not committed; awaiting human gate.

---
time: 08:51 PM
type: feature
breaking_change: true
modules:
  - api
  - database
---

# Summary

P2 — DB foundation. First schema migration (`accounts`, `categories`,
`projects`, `transactions`) + an embedded goose runner applied **at API boot**.
Money is `NUMERIC(18,2)`, single-currency, positive; `direction` classifies and
`is_inflow` signs; transfers are two grouped rows + an optional fee row. sqlc
generation is deferred until the first module adds queries (sqlc already parses
the schema).

# Files Touched

- `api/migrations/00001_init_schema.sql` (new) — four tables, CHECKs, indexes.
- `api/migrations/embed.go` (new) — `//go:embed *.sql` FS.
- `api/internal/store/migrate.go` (new) — `store.Migrate` (goose, embedded).
- `api/cmd/api/main.go` — run migrations at boot before serving; fail fast.
- `api/internal/store/store.go` — `Open` comment: boot now requires a DB.
- `api/go.mod`, `go.sum` — add `github.com/pressly/goose/v3`.
- `docs/wiki/database.md`, `domain.md`, `known-constraints.md` — real schema +
  money/sign conventions + boot rule.
- `docs/wiki/decisions/0002-migrations-at-boot.md` (new ADR).

# Previous Behavior

API booted without a live DB (lazy pool); no schema; `sqlc.yaml` + `migrations/`
inert.

# New Behavior

API embeds and applies migration 00001 at boot via goose, then serves. A fresh
DB self-migrates; an unreachable DB fails boot fast (exit non-zero). `/health`
unchanged.

# Reason For Change

Approved P2: stand up the DB foundation (schema + migration plumbing) as the base
for module CRUD/queries.

# Decisions (user, this session)

- **Amount** `NUMERIC(18,2)`, single currency (IDR).
- **Sign** `direction` enum (income/expense/transfer) + positive amount; balance
  sign via `is_inflow`.
- **Transfers** two linked rows sharing `transfer_group_id` + optional fee row.
- **Migrations run at boot** — supersedes P1 boot-without-DB (ADR-0002).
- **sqlc deferred** — no speculative queries; generation lands with the first
  module (sqlc parses the schema cleanly already).

# Risks

- **Breaking**: the API now requires a reachable DB at boot — deploy must order
  the DB first (ADR-0002).
- Migration not yet applied against a live Postgres (none reachable in this env);
  validated by sqlc DDL parse + `go build` (embed) + boot fail-fast smoke. Live
  apply happens at first deploy / local-DB run.
- Transfer out==in is an app invariant, not a DB CHECK — the service must write
  legs in one tx.

# Notes

- Verified: `gofmt`/`go vet`/`go build` clean; sqlc parsed the schema (failed only
  on no-queries); boot vs unreachable DB → `migrate: up: ... connection refused`,
  exit non-zero, never served.
- goose's modernc/sqlite deps are test-only (not in our binary; `go mod why`).
- Constraints honored: no package godoc; comments ≤2 lines; smallest safe change;
  no public contract (DTO/route/auth) changed.
- Not committed; awaiting human gate. Code review (CodeRabbit) is the next step.

---
time: 09:10 PM
type: feature
breaking_change: false
modules:
  - api
  - database
---

# Summary

P2 follow-up: boot resilience + live verification. Added a bounded connect-retry
to `store.Migrate` (waits up to 30s for the DB, then fails) so a just-restarted
shared-postgres doesn't crash boot, and applied the migration against a real
throwaway Postgres to verify the whole foundation end-to-end.

# Files Touched

- `api/internal/store/migrate.go` — `waitForDB` (30s budget, ctx-cancellable),
  called before `goose.Up`; `dbReadyBudget` const.
- `docs/wiki/database.md`, `known-constraints.md`, `decisions/0002-migrations-at-boot.md`
  — "fails boot fast" → "waits up to 30s, then fails".
- `docs/wiki/deferred-notes.md` — one-shot migrate job + advisory lock recorded as
  deferred (needed only at >1 replica).

# Previous Behavior

Boot migration failed instantly if the DB was momentarily unreachable. Migration
had not been applied against a live Postgres.

# New Behavior

`store.Migrate` retries the DB for 30s before failing. Verified live against a
`kanjo-local` Postgres 17 container: goose applied v1; all 4 tables created;
boot succeeded while Postgres was still starting; `/health` → `{"db":"up"}`.

# Reason For Change

User asked to add the connect retry and to spin a throwaway DB for live testing.

# Risks

- Transfer out==in balance is still an app invariant, not a DB CHECK.
- 30s budget is a fixed ceiling; slower DB starts would still fail (tune if
  shared-postgres boots slower).

# Notes

- Schema invariants verified live: `amount > 0`, `flow_matches_direction`, and
  `transfer_grouped` CHECKs all reject bad rows; account `ON DELETE RESTRICT`
  blocks deleting an account with history.
- Money math verified: income/expense/transfer + bank-fee row → balances
  BCA 493,500 / Emergency 300,000; burn(expense) 206,500 (fee counted, transfer
  legs excluded).
- `kanjo-local` (Postgres 17, no volume, port 5432) left running and data-cleared
  for future tests. `gofmt`/`vet`/`build` clean.
- Not committed; awaiting human gate.

---
time: 09:42 PM
type: feature
breaking_change: false
modules:
  - accounts
  - api
  - database
---

# Summary

P2 — API by module. First business module (**accounts**) as the reference for the
layering: thin handler → service → store (sqlc over pgx). Full CRUD under
`/api/v1/accounts`. This pass also **wires sqlc** (was inert): `accounts.sql` →
generated `internal/store/db/`, embedded on `Store` as `*db.Queries`. Bare-JSON
contract + `{"error":"…"}` (matches `/health`), feature-first package layout —
both confirmed with the user before coding.

# Files Touched

- `api/internal/store/queries/accounts.sql` (new) — List/Get/Create/Update/Delete.
- `api/internal/store/db/` (new, generated) — `db.go`, `models.go`,
  `accounts.sql.go` (sqlc v1.31.1, `DO NOT EDIT`).
- `api/internal/store/store.go` — embed `*db.Queries` via `db.New(pool)`; keep
  pool for ping/tx.
- `api/internal/store/errors.go` (new) — `ErrNotFound`/`ErrConflict`/`ErrInUse` +
  `Classify` (pgx.ErrNoRows → NotFound; `23505` → Conflict; `23503` → InUse).
- `api/internal/account/service.go` (new) — validation (trim, ≤100 runes),
  `Account` DTO, `Input` (pointer `is_liquid` ⇒ default true), row→DTO, error xlate.
- `api/internal/account/handler.go` (new) — decode/validate/path-id, call service,
  sentinel→HTTP mapping, local JSON helpers.
- `api/internal/account/service_test.go` (new) — `cleanName` / `liquidOrDefault`.
- `api/internal/server/router.go` — `account…Mount(mux)`; comment update.
- `docs/wiki/modules/accounts.md` (new) — module contract (routes, shapes, taxonomy).
- `docs/wiki/database.md` — sqlc now wired (was "not yet wired") + `go run` codegen cmd.

# Previous Behavior

Router served only `GET /health`; `/api/v1` empty. sqlc inert (no queries, no
`db` pkg). `Store` exposed only pool/Ping/Close.

# New Behavior

`/api/v1/accounts` full CRUD. List ordered by name; create defaults `is_liquid`
true; dup name → 409; not found / absent update id → 404; bad id/JSON → 400;
delete with transactions → 409 (RESTRICT); delete ok → 204. `/health` unchanged.

# Reason For Change

Approved P2 task: stand up the first module and lock the handler→service→store
pattern + sqlc wiring + the JSON/error contract for every later module.

# Decisions (user, this session)

- **Scope** accounts only (reference slice); categories/projects replicate next,
  transactions separate.
- **Full CRUD**; **bare JSON + `{"error"}`** (no envelope); **feature-first**
  layout (`internal/<module>/`).
- **Surrogate PKs stay `BIGINT IDENTITY`** (not UUID/ULID): single-writer, index
  locality, no enumeration risk, zero deps. UUID kept only where it earns it
  (`transfer_group_id`). Captured here, not as an ADR (caller declined the ADR).
- **DTO ≠ generated row** — wire contract decoupled from sqlc/schema regen.
- **sqlc via `go run …@v1.31.1`** — no global install; output committed.

# Risks

- Public contract created (routes, JSON, error taxonomy) — later modules must
  follow it; changing it is a breaking change.
- Generated `internal/store/db/` is committed; regenerate (don't hand-edit) when
  queries/schema change.
- JSON helpers duplicated in `account` pkg (ponytail-marked) — lift to
  `internal/httpx` when the 2nd module needs them.
- Transfer out==in invariant still app-enforced (not this module's concern yet).

# Notes

- Verified live vs `kanjo-local`: every CRUD path + all error codes
  (400/404/409/204), incl. RESTRICT 409 (seeded a transaction, then delete).
  Seed rows cleared after. `gofmt`/`vet`/`build`/`go test` clean.
- Constraints honored: no package godoc; comments ≤2 lines; smallest safe slice;
  `/health` untouched; no broad refactor.
- Not committed; awaiting human gate. CodeRabbit review is the next step.

---
time: 11:00 PM
type: feature
breaking_change: false
modules:
  - categories
  - projects
  - api
---

# Summary

Two near-identical id/name CRUD modules — **categories** and **projects** —
copied from the accounts reference shape (thin handler → service → store, sqlc
over pgx). Simpler than accounts: no `is_liquid`, and delete is
`ON DELETE SET NULL` (not RESTRICT) so there is **no in-use 409 path**. Same pass
lifted the duplicated JSON/request plumbing out of `account/handler.go` into a new
`internal/httpx` package (rule of three: account + category + project + the
router's health handler all needed it).

# Files Touched

- `api/internal/store/queries/categories.sql`, `projects.sql` (new) — 5 queries each.
- `api/internal/store/db/categories.sql.go`, `projects.sql.go` (new, sqlc-generated).
- `api/internal/httpx/httpx.go` (new) — `WriteJSON`, `WriteErr`, `PathID`,
  generic `Decode[T]`; the bare-JSON + `{"error"}` contract in one place.
- `api/internal/category/{service,handler,service_test}.go` (new).
- `api/internal/project/{service,handler,service_test}.go` (new).
- `api/internal/account/handler.go` — use `httpx.*`; dropped the 4 local helpers
  (`writeJSON`/`writeErr`/`decode`/`pathID`) and the ponytail TODO they carried.
- `api/internal/server/router.go` — mount category + project; `health` uses
  `httpx.WriteJSON`; dropped its local `writeJSON`.
- `docs/wiki/modules/categories.md`, `projects.md` (new) — contracts (deltas only).
- `docs/wiki/database.md` — sqlc now wired for categories/projects too.

# Previous Behavior

Only `/api/v1/accounts` had CRUD. `categories`/`projects` tables existed (migration
00001) but had no queries, no module, no routes. JSON/request helpers lived
duplicated in `account/handler.go` (and `writeJSON` again in `router.go`).

# New Behavior

`/api/v1/categories` and `/api/v1/projects` full CRUD: list ordered by name,
create with unique-name 409, blank/too-long name 400, not-found/absent-id 404,
bad id/JSON 400, delete 204. Deleting a category/project referenced by a
transaction succeeds (204) and nulls the transaction's `category_id`/`project_id`
(SET NULL) — no 409. `/health` and `/api/v1/accounts` behavior unchanged. Shared
helpers now live in `internal/httpx`.

# Reason For Change

Approved task: replicate the accounts shape for the two remaining simple
reference tables, and take the natural rule-of-three trigger to lift the JSON
helpers into `internal/httpx`.

# Decisions (user, this session)

- **httpx scope** — lift all four request/response helpers (`WriteJSON`,
  `WriteErr`, `PathID`, `Decode[T]`), not just the two JSON writers, since all are
  domain-free plumbing that would otherwise triplicate.
- **`cleanName`/validation kept per-module** (copied), not shared — faithful to
  "copy the accounts shape"; lifting validation was out of scope.
- **No `ErrInUse` case** in category/project `fail()` — unreachable under SET NULL;
  omitting it keeps the handler honest (F-005's 23503 never fires for them).
- **`store.Classify` untouched** — F-005 mapping stays as documented.

# Risks

- Public contract widened (two new route groups + the shared `httpx` contract);
  later modules must follow it.
- Generated `internal/store/db/` recommitted (now includes categories/projects);
  regenerate (don't hand-edit) on schema/query change.
- Account handler now depends on `internal/httpx`; a change there touches all
  three modules + health.

# Notes

- Verified live vs `kanjo-local` (Postgres 17): every CRUD path + all error codes
  (201/200/204/400/404/409) for both modules; SET NULL confirmed end-to-end —
  seeded account+category+project+transaction, deleted category and project via
  API (both 204), transaction row survived with `category_id`/`project_id` NULL.
  Seed rows truncated after; API stopped.
- `gofmt`/`go vet`/`go build`/`go test ./...` all clean.
- Constraints honored: no package godoc; comments ≤2 lines; smallest safe slice;
  `/health` + accounts untouched (helper swap only); no broad refactor.
- Not committed; awaiting human gate. CodeRabbit review is the next step.

---
time: 11:55 PM
type: feature
breaking_change: false
modules:
  - transactions
  - api
  - database
---

# Summary

Transactions **2a — single-entry CRUD** (income/expense rows). Same layering as
accounts (thin handler → service → store, sqlc over pgx) plus the harder bits the
module owns: money parse/validate (NUMERIC(18,2), positive, ≤2 places, decimal
**string** on the wire — no float), optional list filters (date range +
account/category/project, ordered newest-first), and FK **pre-validation** —
**F-005's real fix**. Transfers (2b) deferred to a separate pass; this slice is
designed so 2b needs no rework.

# Files Touched

- `api/internal/store/queries/transactions.sql` (new) — List (optional `sqlc.narg`
  filters)/Get/Create/Update/Delete; Update+Delete scoped to
  `transfer_group_id IS NULL`.
- `api/internal/store/db/transactions.sql.go` (new, sqlc-generated).
- `api/internal/transaction/money.go` (new) — `parseAmount`/`formatAmount`
  (big.Int mantissa + base-10 exp; cents never drift).
- `api/internal/transaction/service.go` (new) — DTO/`Input`/`Filter`, validate,
  `flowFor` (is_inflow derived), generic `requireRef` FK pre-validate, row→DTO.
- `api/internal/transaction/handler.go` (new) — CRUD + `parseFilter`/`optID`;
  bad FK → 400, tx not found → 404, **no 409 path**.
- `api/internal/transaction/service_test.go` (new) — money round-trip + bounds,
  `flowFor`, `parseDate`, `normalizeTags`.
- `api/internal/server/router.go` — mount transactions.
- `docs/wiki/modules/transactions.md` (new) — module contract.
- `docs/wiki/resolutions.md` — R-005 follow-up: the deferred FK fix landed here.

# Previous Behavior

`transactions` table existed (migration 00001) but had no queries, module, or
routes. F-005's 23503→`ErrInUse` mapping had no live writer of an outbound FK, so
a bad insert FK could only have surfaced (later) as a wrong 409 "in use".

# New Behavior

`/api/v1/transactions` full CRUD for income/expense rows. List supports
`from`/`to`/`account_id`/`category_id`/`project_id` filters, ordered
`occurred_on DESC, id DESC`. `direction` is income|expense only (transfer
rejected, 400); `is_inflow` derived. Bad `account_id`/`category_id`/`project_id`
on write → **400** (pre-validated), never 409. Transfer legs are immutable via
this API (Update/Delete scoped to non-transfer rows → 404); reads still show them.
`/health` + accounts/categories/projects unchanged.

# Reason For Change

Approved task: the transactions module (2a single-entry), including F-005's real
fix (outbound FK on insert must be 400/404, not 409).

# Decisions (user, this session)

- **Scope 2a only** — transfers (2b) a separate reviewed pass.
- **Amount = decimal string** on the wire (not JSON number) — money precision;
  sets the contract for all later money fields.
- **FK fix = pre-validate in service** (the F-005/R-005-anticipated branch), not
  splitting `store.Classify` — keeps the shared classifier and the other modules
  untouched; blast radius = this module.
- **Pagination deferred** (single-user MVP; matches existing unbounded lists).
- **`Store.WithTx` lands in 2b** (first real use) — no unused helper in 2a.

# Risks

- Public contract widened (new route group + money-as-string + filter params);
  later money fields should follow the string convention.
- Residual race: an FK deleted between pre-validate and insert leaks a 23503 →
  500 (logged). Effectively impossible single-user; acceptable.
- Generated `internal/store/db/` recommitted (now includes transactions);
  regenerate (don't hand-edit) on schema/query change.

# Notes

- Verified live vs `kanjo-local` (Postgres 17): every CRUD path + filters; money
  canonicalized to 2dp; tags trimmed/blank-dropped; `is_inflow` derives and flips
  on update. All error codes (201/200/204/400/404). F-005 confirmed: bad
  account/category/project FK on POST → 400 (not 409). Transfer-leg immutability:
  seeded a grouped `transfer` row, Update/Delete → 404, leg survives, UUID renders
  canonical. Seed rows truncated; API stopped.
- `gofmt`/`go vet`/`go build`/`go test ./...` all clean.
- Constraints honored: no package godoc; comments ≤2 lines; smallest safe slice;
  no broad refactor; `store.Classify` + sibling modules untouched.
- Not committed; awaiting human gate. CodeRabbit review is the next step.
