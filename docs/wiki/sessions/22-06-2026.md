---
time: 12:25 AM
type: feature
breaking_change: false
modules:
  - transactions
  - api
  - database
---

# Summary

Transactions **2b — transfers**. A transfer is a group of rows sharing one
`transfer_group_id` (a v4 UUID the app generates), written in **one transaction**
via the new `Store.WithTx`: two `transfer` legs (out of source, into destination,
equal amount ⇒ out==in by construction) plus an optional `expense` fee row charged
to the source. Routes: `POST/GET/DELETE /api/v1/transfers`. Same package as 2a,
reusing its money/DTO/validation helpers.

# Files Touched

- `api/internal/store/queries/transactions.sql` — +`CreateGroupedTransaction`
  (inserts a row carrying a group id), `GetTransferGroup`, `DeleteTransferGroup`.
- `api/internal/store/db/transactions.sql.go` — regenerated (sqlc).
- `api/internal/store/store.go` — `WithTx(ctx, fn func(*db.Queries) error) error`
  (begin/commit/rollback; binds the generated queries to the tx). First real use
  of the tx pattern (deferred here from 2a).
- `api/internal/transaction/transfer.go` (new) — `TransferInput`/`TransferResult`,
  `CreateTransfer`/`GetTransfer`/`DeleteTransfer`, pure `transferRows` builder,
  `newGroupID` (v4 UUID from `crypto/rand`, no dep).
- `api/internal/transaction/transfer_handler.go` (new) — routes, `parseGroupID`
  (UUID path), `failTransfer` (missing group → 404 "transfer not found").
- `api/internal/transaction/handler.go` — mount transfer routes; extend `fail`'s
  400 case with the transfer sentinels.
- `api/internal/transaction/service.go` — extract `uuidString`; add
  `requireAccountErr` (custom sentinel) so transfers distinguish from/to.
- `api/internal/transaction/transfer_test.go` (new) — `transferRows` (out==in, fee
  shape/account/category), `newGroupID` v4 bits + uniqueness.
- `docs/wiki/modules/transactions.md` — Transfers section.

# Previous Behavior

Only single-entry CRUD (2a) existed. Transfers could not be created (single-entry
`Create` rejects `direction=transfer` and never sets a group id), and a transfer
leg seeded directly in the DB could not be deleted via the API (single-entry
delete is scoped to `transfer_group_id IS NULL`). `Store` had no tx helper.

# New Behavior

`POST /api/v1/transfers` writes 2–3 grouped rows atomically and returns
`{transfer_group_id, transactions[]}`. `GET /transfers/{group_id}` returns the
group; `DELETE /transfers/{group_id}` removes the whole group in one statement
(the only way to delete a transfer). Transfers are immutable (no PUT). FKs
(from/to account, fee category) are pre-validated → 400 on a bad ref, never 409.
`/health` and every other route unchanged.

# Reason For Change

Approved task: the transfers half of the transactions module (2b), the
logic-heavy multi-row, single-tx write.

# Decisions (user, this session)

- **Fee charged to source** (`from_account_id`), `direction=expense`.
- **Transfers immutable** — no `PUT /transfers`; edit = delete + recreate. Update
  deferred.
- **UUID app-side** via `crypto/rand` v4 (both legs share it) — no new dependency,
  no `gen_random_uuid()` round-trip.
- **Response = `{transfer_group_id, transactions[]}`**, reusing the `Transaction`
  DTO + `toTransaction` (no parallel money/format code).

# Risks

- out==in is an **app invariant**, held by writing both legs with the same amount
  in one tx — not a DB CHECK. Any future writer of transfer rows must preserve it.
- Rollback path is standard (`WithTx` rolls back on any insert error) but not
  reachable via the API in normal use: every FK and amount is pre-validated before
  the tx opens, so the inserts don't fail. Covered by the unit test on the pure
  `transferRows` builder + the helper's begin/commit/rollback.
- Public contract widened (new `/transfers` route group + `TransferInput`).
- Generated `internal/store/db/` recommitted (now includes the grouped queries).

# Notes

- Verified live vs `kanjo-local` (Postgres 17): no-fee transfer → 2 legs (out
  acct=from inflow=false, in acct=to inflow=true, equal amount, shared group);
  with-fee → 3 rows (fee=expense, inflow=false, charged to source, category +
  tags carried). `GET` group → 200; balances signed by `is_inflow` matched exactly
  (from −806500, to +800000 across two transfers incl. a 6500 fee). All error
  codes: same from==to / bad from / bad to / bad fee / unknown fee category /
  missing amount / bad date / bad JSON → 400; bad UUID → 400; unknown group
  GET/DELETE → 404. `DELETE` group → 204 removed all 3 rows of the target group
  while the other group's legs survived. Single-entry delete on a leg still 404.
  Seed truncated; API stopped.
- `gofmt`/`go vet`/`go build`/`go test ./...` all clean.
- Constraints honored: no package godoc; comments ≤2 lines; no new dependency;
  smallest safe slice; `store.Classify` + sibling modules untouched; no broad
  refactor.
- Committed `f9efaa8`. CodeRabbit (`--base-commit <2a>`): 1 minor (fee NULL
  category) — see the follow-up below.

---
time: 12:55 AM
type: fix
breaking_change: false
modules:
  - transactions
---

# Summary

Require `fee_category_id` when a transfer carries a `fee` (reverses the initial
F-008 wontfix). The user flagged that an uncategorized fee silently inflates the
"Uncategorized" bucket in burn / Category Breakdown — and the fee is the only
report-visible row of a transfer (the legs are `transfer`, excluded from both
reports). So a fee must now be categorized.

# Files Touched

- `api/internal/transaction/transfer.go` — new sentinel `ErrFeeCategoryRequired`;
  `validateTransfer` rejects a fee with no `fee_category_id`.
- `api/internal/transaction/handler.go` — map the sentinel to 400.
- `docs/wiki/modules/transactions.md` — fee category now required-with-fee.
- `docs/wiki/findings.md`, `resolutions.md` — F-008 flipped to fixed; R-008
  rewritten (was wontfix).

# Previous Behavior

A transfer `fee` without `fee_category_id` was accepted (201) and wrote the fee
`expense` row with `category_id = NULL`.

# New Behavior

A `fee` with no `fee_category_id` → **400** `fee_category_id is required when a fee
is set`. With a valid category → 201, fee row categorized. No-fee transfers
unaffected. The lone divergence from single-entry expenses (which still allow a
null category) — intentional, because the transfer fee is auto-generated.

# Reason For Change

User concern: transfer fees were landing in "Uncategorized" for reporting.

# Risks

- Minor contract tightening: a previously-accepted body (fee, no category) is now
  a 400. No persisted data affected (verified DBs were throwaway).

# Notes

- Verified live vs `kanjo-local`: fee + no category → 400; fee + category → 201
  (fee row `category_id` set, legs null); no-fee → 201 unaffected. Seed truncated;
  API stopped. `gofmt`/`vet`/`build`/`test ./...` clean.
- Not committed yet.

---
time: 10:26 AM
type: feature
breaking_change: false
modules:
  - reports
  - api
  - database
---

# Summary

Phase 3 — **reports / derived views**. New read-only `report` package exposing
five `/api/v1/reports` endpoints, all derived from `transactions` on each request
(nothing stored). Covers the eight derived views from `domain.md`: current
balance, monthly burn, savings rate, runway, cash flow, category breakdown,
project cost, balance trend. Hybrid grouping (user-approved): one `summary`
snapshot + four series/breakdown endpoints.

# Files Touched

- `api/internal/store/queries/reports.sql` (new) — 6 aggregates: `BalanceTotals`,
  `WindowTotals`, `CashFlowByPeriod`, `CategoryTotals`, `ProjectTotals`,
  `BalanceByPeriod`. Every sum `COALESCE(SUM(...),0)::numeric` (else sqlc types it
  `interface{}` and NULL on empty).
- `api/internal/store/db/reports.sql.go` (new) — sqlc-generated.
- `api/internal/report/service.go` (new) — 5 methods; money/ratio via `big.Rat`
  (no float on the wire); window resolution (trailing-3-month default); `interval`
  allowlist {day,week,month}.
- `api/internal/report/handler.go` (new) — routes, query-param parse, error map
  (400 bad date/interval; 500 otherwise — no 404/409, reports only read).
- `api/internal/report/service_test.go` (new) — `numToRat`/`money` round-trip,
  `ratio` null-on-zero-denominator, `monthSpan`, `validBucket`, `resolveWindow`
  (explicit + trailing-3-month default), `fromInt8`.
- `api/internal/server/router.go` — `report…Mount(mux)` (+1 line + import).
- `docs/wiki/modules/reports.md` (new) — module contract.

# Previous Behavior

No reports module. Balance/burn/runway/cash-flow/etc. existed only as concepts in
`domain.md`; no endpoint computed them. `/api/v1` ended at transactions/transfers.

# New Behavior

`GET /api/v1/reports/{summary,cash-flow,categories,projects,balance-trend}` return
derived figures. `summary` = current/liquid balance (all-time stock) + windowed
income/expense/net/savings_rate/monthly_burn/runway_months. Series endpoints
bucket by `date_trunc` (`interval` day/week/month). Money is decimal strings;
`savings_rate`/`runway_months` are `null` when their denominator is 0. List
endpoints return `[]` (never `null`) when empty. No existing route changed.

# Reason For Change

Approved task: Phase 3 reports — the read-only aggregation layer.

# Decisions (user, this session)

- **Hybrid 5 endpoints** (summary + 4) over fully-discrete-8 or minimal-3.
- **Runway monthly burn = trailing 3 calendar months** average (window-driven when
  `from`/`to` supplied); `runway = liquid_balance / burn`, null when burn 0.
- **Decimal strings + null** for money and ratios (no float on the wire).

# Risks

- **Aggregation contracts must hold:** balances sum **all** rows signed by
  `is_inflow` (transfers included); flow reports filter `direction IN
  (income,expense)` (transfers excluded, fee counts). Any new report query must
  pick the right side or numbers drift. Documented in `modules/reports.md`.
- `balance-trend` ignores `from` by design (cumulative needs full history); only
  `to` bounds it. A future windowed-trend would need an opening-balance carry.
- Public contract widened (new `/reports` route group); no schema change, no new
  dependency (`math/big` is stdlib), `store.Classify` + sibling modules untouched.
- Generated `internal/store/db/` recommitted (now includes the report queries).

# Notes

- Verified live vs a throwaway Postgres 17 (kanjo-local was stopped and 5432 is
  held by another project's `local-postgres`; spun an isolated `kanjo-smoke` on
  5433, `--rm`, removed after). Seed: 3 accounts (2 liquid), 4 categories, 1
  project, 4 single entries + 1 transfer-with-fee. All figures matched by hand:
  current/liquid balance 7,308,500.00; income 10,000,000 / expense 2,691,500;
  savings_rate 0.7309; monthly_burn 897,166.67; runway 8.15; cash-flow &
  balance-trend (cumulative ends at current balance) correct; categories desc with
  the fee categorized; project cost 185,000. Branches: savings_rate null (no
  income), runway null (zero expense), bad date/interval → 400, week (Monday) /
  day buckets, `to` bound, empty DB → zeros/null/`[]`. Container removed; `:3000`
  freed.
- `gofmt`/`go vet`/`go build`/`go test ./...` all clean.
- Constraints honored: no package godoc; comments ≤2 lines unless reasoned;
  smallest safe slice; no new dependency; `store.Classify` + sibling modules
  untouched; no broad refactor; money-format helper kept local to `report`
  (minimal copy) rather than refactoring `transaction/money.go`.
- Not committed yet. CodeRabbit review pending.

---
time: 11:06 AM
type: feature
breaking_change: false
modules:
  - web
---

# Summary

Phase 4 **S0 — web SPA scaffold** (first of an approved S0→S5 slice plan; deploy
artifacts deferred to a later phase). New `web/` at repo root: Vite 8 + React 19 +
TypeScript, Tailwind v4 (CSS-first), shadcn (vendored, copied-in), React Router 7,
TanStack Query v5, Lucide. Just the foundation + app shell — no feature pages yet;
all six routes render a shared placeholder.

# Files Touched

- `web/` (new) — Vite React-TS baseline (`pnpm create vite`), then customized.
- `web/vite.config.ts` — `@tailwindcss/vite`, `@` → `src` alias, dev proxy
  `/api`+`/health` → `:3000` (single-origin, no CORS, mirrors the nginx edge).
- `web/tsconfig.json` / `tsconfig.app.json` — `@/*` path alias (no `baseUrl`;
  deprecated in TS 6).
- `web/components.json` (new) — shadcn config (new-york, neutral, css vars).
- `web/src/index.css` — Tailwind v4 `@import` + Kanjo paper palette tokens
  (`:root` + `@theme inline`); positive/negative added beyond the shadcn set.
- `web/src/lib/{utils,api,query}.ts` (new) — `cn`; single fetch boundary
  (`api.get/post/put/del`, `ApiError`, `{error}` parsing, 204 handling);
  `QueryClient` (staleTime 30s, retry 1, no refetch-on-focus).
- `web/src/types.ts` (new) — full DTO mirror of the API wire contract.
- `web/src/main.tsx` — `QueryClientProvider` + `BrowserRouter`.
- `web/src/App.tsx` — routes (dashboard/ledger/accounts/categories/projects/
  reports) under a `Layout`, unknown → redirect home.
- `web/src/components/{Layout,HealthBadge,Placeholder}.tsx` (new) — paper sidebar
  nav; live `/health` badge (30s poll); slice placeholder.
- `web/src/components/ui/button.tsx` (new) — shadcn primitive (toolchain canary).
- `web/eslint.config.js` — exempt `src/components/ui/**` from the fast-refresh
  rule (vendored shadcn exports cva variants alongside the component).
- `docs/wiki/design-system.md` — drift fix: tokens live in `index.css` (v4), no
  `tailwind.config.ts`.

# Previous Behavior

No `web/`. Repo was API + docs only; the SPA existed only as a plan in
`architecture.md` / `design-system.md`.

# New Behavior

`pnpm dev` serves the SPA shell at the paper-styled layout with working nav and a
live API-health badge; `pnpm build` (tsc + vite) and `pnpm lint` are clean. Every
route resolves to a placeholder pending its feature slice. No API change.

# Reason For Change

Approved Phase 4, sliced delivery — S0 lays the foundation so each later vertical
(S1 accounts → S5 dashboard) drops in as a small reviewable diff.

# Risks

- New surface only; nothing existing touched besides one doc-drift line.
- Doc/stack choices to hold to: Tailwind v4 CSS-first (no config file); shadcn is
  copied-in, not a dependency; `@/` alias relies on `paths` without `baseUrl`.
- Webfonts not bundled yet (Geist / Noto Serif JP / JetBrains Mono fall back to
  system faces) — a later polish item, noted so mono-numeric columns aren't
  assumed live. `public/favicon.svg` is still the Vite placeholder.

# Notes

- Stack: Vite 8, React 19, TS 6, Tailwind 4, RR 7, TanStack Query 5, pnpm.
- Verified: `pnpm build` green (300 kB / 95 kB gz), `pnpm lint` clean, dev server
  boots and serves `<title>Kanjo</title>` + `#root` + the HMR entry.
- Constraints honored: smallest safe slice (scaffold only, no features); shadcn
  primitives deferred to when a slice needs them (only `button` added as a canary);
  no deploy artifacts (deferred per approval); no API/docs rewrites.
- Not committed yet. CodeRabbit review pending (holding until S1 — S0 is mostly
  generated config).

---
time: 11:45 AM
type: feature
breaking_change: false
modules:
  - web
  - accounts
---

# Summary

Phase 4 **S1 — accounts CRUD** (first real web vertical). New
`web/src/features/accounts/`: TanStack Query hooks over `/api/v1/accounts`, a
data table, a create/edit dialog (React Hook Form + Zod), and a delete
confirmation. Proves the SPA's data layer end-to-end against the live API.

Also sets the **per-phase cadence** the user asked for: build → CodeRabbit review
→ discuss/fix findings → commit, one commit per phase. S0 was committed under this
cadence as `d152bcb` (superseding the S0 entry's "not committed yet" line above).

# Files Touched

- `web/src/features/accounts/hooks.ts` (new) — `useAccounts` (list) +
  `useCreate/Update/DeleteAccount` mutations, all invalidating `["accounts"]`.
- `web/src/features/accounts/AccountDialog.tsx` (new) — RHF+Zod form (name ≤100
  mirrors the API; `is_liquid` Switch defaulting true); create vs edit by prop;
  toasts on success, `ApiError.message` on failure.
- `web/src/features/accounts/AccountsPage.tsx` (new) — table (Name / Liquid badge /
  Created / row actions), New-account button, AlertDialog delete confirm, loading/
  error/empty states.
- `web/src/App.tsx` — `/accounts` route now renders `AccountsPage` (was placeholder).
- `web/src/main.tsx` — mount `<Toaster />` (sonner).
- `web/src/components/ui/*` — shadcn primitives added (table, dialog, alert-dialog,
  input, switch, sonner, form, label, badge); `sonner.tsx` rewritten to drop
  `next-themes` (single light theme).
- `web/package.json` — +react-hook-form, zod, @hookform/resolvers, radix-ui;
  −next-themes (F-014); `tsconfig.app.json` — `strict: true` (F-016).
- `docs/wiki/findings.md` / `resolutions.md` — F-012…F-016 ↔ R-012…R-016.

# Previous Behavior

`/accounts` rendered a placeholder. No web code talked to the API; the data layer
(fetch boundary, query client) existed in S0 but was unexercised.

# New Behavior

Accounts page lists accounts, creates/edits via dialog, and deletes with
confirmation — optimistic-free, refetch-on-invalidate. Server errors (409 name
conflict, 409 in-use, 400 validation, 404) surface as toasts carrying the API's
`{error}` message; client-side Zod blocks empty/over-long names before submit.

# Reason For Change

Approved Phase 4 slice S1 — the first feature vertical, also the template the
remaining CRUD slices (S2 categories/projects) follow.

# Decisions (user, this session)

- **Per-phase cadence**: CodeRabbit review then commit, one commit per phase
  (saved to memory). Overrode the earlier "defer review to S1" pick.
- **Strict mode on** — scaffold had it off; enabled now (money-app null safety).
- **Vendored wontfixes** — F-012 (false positive), F-013 (dead guard, upstream),
  F-015 (`use client` no-op) left unpatched; shadcn primitives are copied-in and
  shouldn't drift from regen.

# Risks

- New surface only; no API/contract/migration change. `radix-ui` unified package
  is now a dependency (shadcn's current convention).
- Bundle grew (~486 kB / 150 kB gz) with radix primitives — acceptable for an
  internal tool; code-split later if it matters.
- Vendored `sonner.tsx` was hand-edited (next-themes removal) — a future
  `shadcn add`/regen of sonner would need the same edit reapplied. Noted.

# Notes

- **Live smoke** vs a throwaway Postgres 17 (`kanjo-s1-smoke` on 5433, `--rm`;
  5432 is another project's `local-postgres`) with the API on :3000 and the SPA's
  Vite proxy — full accounts cycle through `/api/v1`: list `[]` → create (omitted
  `is_liquid` → true) → create illiquid → list → update rename+flip → duplicate
  409 → blank-name 400 → delete 204 → missing 404. Container removed, ports freed.
- CodeRabbit (`-t uncommitted`): 5 findings (1 critical false-positive, 1 major
  vendored, 3 minor). 2 fixed (F-014, F-016), 3 wontfix. `pnpm build`/`pnpm lint`
  green under strict.
- Constraints honored: smallest safe slice; vendored primitives not patched
  (except the necessary next-themes drop); no API touched; no deploy artifacts.
- Committed this phase (S1) as `76bd58a`.

---
time: 01:35 PM
type: feature
breaking_change: false
modules:
  - web
  - categories
  - projects
---

# Summary

Phase 4 **S2 — categories + projects**. Both are structurally identical name-only
buckets, so instead of two near-duplicate pages they share one parameterized
manager `features/shared/NameCrud.tsx` (table + create/edit dialog + delete
confirm), wired twice from the router with per-resource labels. Accounts stays
bespoke (it carries `is_liquid`).

# Files Touched

- `web/src/features/shared/NameCrud.tsx` (new) — generic name-only CRUD: own
  TanStack Query hooks (list + create/update/delete invalidating `[path]`),
  RHF+Zod single-name form (≤100), AlertDialog delete. Props: `path`, `title`,
  `subtitle`, `singular`, `placeholder`.
- `web/src/App.tsx` — `/categories` and `/projects` routes now render `<NameCrud>`
  with their config (were placeholders).
- `docs/wiki/findings.md` / `resolutions.md` — F-017 ↔ R-017.

# Previous Behavior

`/categories` and `/projects` rendered placeholders; only accounts had a real page.

# New Behavior

Categories and projects each list/create/edit/delete via the shared manager.
Delete copy reflects the `ON DELETE SET NULL` contract ("transactions keep their
history — they just lose this category/project"), and there is no in-use 409 for
these resources (unlike accounts). Name conflicts (409) and validation (400)
surface as toasts; Zod blocks blank/over-long names client-side.

# Reason For Change

Approved Phase 4 slice S2. Chosen design: one shared component over two copies,
since the two resources are genuinely identical — the repetition that justifies
the abstraction (per clean-code/ponytail: extract on real repetition, not before).

# Decisions (user, this session)

- **Shared `NameCrud`** for the two identical resources; **accounts left bespoke**
  (extra field) — deliberately not over-unified.
- **F-017 fixed in-scope only** — the same Cancel-during-delete nit in committed
  `AccountsPage` was left to avoid mixing a fix into an unrelated diff.

# Risks

- New surface only; no API/contract/migration change.
- `NameCrud` owns its hooks inline (path-parameterized) rather than via a hook
  factory — chosen to stay clear of React's rules-of-hooks with closures, at the
  cost of accounts not sharing the exact same hook code.

# Notes

- **Live smoke** vs throwaway Postgres 17 (`kanjo-s2-smoke` on 5433, `--rm`), API
  :3000, SPA Vite proxy — full cycle for **both** resources through `/api/v1`:
  list `[]` → create ×2 → duplicate 409 (resource-named message) → blank 400 →
  update → delete 204 → missing 404. Container removed, ports freed.
- CodeRabbit (`-t uncommitted`): 1 minor (F-017), fixed. `pnpm build`/`pnpm lint`
  green under strict.
- Constraints honored: smallest safe slice; no API touched; committed sibling code
  untouched; no deploy artifacts.
- Committed this phase (S2) as `090d49a`.

---
time: 01:55 PM
type: feature
breaking_change: false
modules:
  - web
  - transactions
---

# Summary

Phase 4 **S3 — transactions** (the ledger). New `features/transactions/`: a
filterable ledger table, a single-entry create/edit dialog, and a separate
create-only transfer dialog. First slice to render money and to touch the
transfer flow. Also adds two shared bits — `lib/money.ts` (IDR display) and
`lib/resources.ts` (`useResourceList` for FK selects/filters, cache-shared with
the CRUD pages).

# Files Touched

- `web/src/lib/money.ts` (new) — `formatAmount` (Intl IDR, display only; decimal
  strings never parsed to float for math).
- `web/src/lib/resources.ts` (new) — `useResourceList<T>(path)`, queryKey `[path]`
  (shares cache/invalidation with accounts/categories/projects pages).
- `web/src/features/transactions/hooks.ts` (new) — `useTransactions(filter)`
  (query-string built from the filter), create/update/delete transaction +
  create/delete transfer; every write invalidates `["transactions"]`.
- `web/src/features/transactions/form.ts` (new) — shared `NONE` sentinel, date +
  amount Zod rules (mirror NUMERIC(18,2) > 0), `splitTags`, `todayStr`.
- `web/src/features/transactions/TransactionDialog.tsx` (new) — RHF+Zod single
  entry (date, direction income|expense, amount, account, optional category/
  project, description, comma tags).
- `web/src/features/transactions/TransferDialog.tsx` (new) — create-only transfer
  (from/to, amount, optional fee + required fee-category, description, tags);
  client refinements: from≠to, fee⇒fee-category.
- `web/src/features/transactions/LedgerPage.tsx` (new) — table (Date, Description,
  Amount signed/colored by `is_inflow`, Account, Category, Project, Tags), date +
  account/category/project filters, New-transaction / New-transfer, edit (single
  entries only), delete (single → `/transactions/{id}`, transfer → whole group via
  `/transfers/{group_id}`).
- `web/src/components/ui/{select,textarea}.tsx` — shadcn primitives added.
- `web/src/App.tsx` — `/ledger` → `LedgerPage`.
- `docs/wiki/findings.md` / `resolutions.md` — F-018, F-019 ↔ R-018, R-019.

# Previous Behavior

`/ledger` was a placeholder. No money rendering, no transfer UI.

# New Behavior

The ledger lists transactions (filterable by date range + account/category/
project), creates/edits single income/expense entries, and creates transfers
(2 legs + optional fee). Transfer rows are tagged and **not** editable (immutable
by contract); deleting one removes the whole group. Amounts render in the mono
face, signed and colored by `is_inflow`. Server validation (bad FK, same-account,
fee-without-category, etc.) surfaces as toasts; Zod blocks bad input client-side.

# Reason For Change

Approved Phase 4 slice S3 — the transactions/ledger vertical, the logic-heavy
center of the app.

# Decisions (user, this session)

- **Native `<input type="date">`** over a date-picker dep; **comma-separated tag
  input** over a chip widget (ponytail — native/simple first).
- **Two dialogs** (single entry vs transfer) reflecting the two API shapes;
  transfers create-only.
- **F-018/F-019 wontfix** — both on the vendored React-19 textarea (forwardRef
  unnecessary under React 19; field-sizing is progressive enhancement).

# Risks

- New surface only; no API/contract/migration change.
- `formatAmount` uses `Number()` — exact for IDR amounts under 2^53; flagged in
  the file for a decimal lib if ever exceeded.
- Transfer rows rendered from the same list as single entries; edit is hidden and
  delete routes to the group endpoint — the only safe operations on a leg.

# Notes

- **Live smoke** vs throwaway Postgres 17 (`kanjo-s3-smoke` on 5433, `--rm`), API
  :3000, SPA Vite proxy — seeded accounts/categories/project, then through
  `/api/v1`: create income + expense (category/project/tags), filter by category /
  date, update, transfer-with-fee → exactly 3 rows (out leg acct1, in leg acct2,
  fee expense acct1/cat), delete group → 204 (all 3 gone, originals remain),
  delete single → 204, missing → 404; 400s for fee-without-category, same-account,
  bad FK. Container removed, ports freed.
- CodeRabbit (`-t uncommitted`): 2 findings, both vendored textarea, both wontfix.
  `pnpm build`/`pnpm lint` green under strict.
- Constraints honored: smallest safe slice; vendored primitives unpatched; no API
  touched; no deploy artifacts.
- Committed this phase (S3) as `20f507b`.

---
time: 02:25 PM
type: feature
breaking_change: false
modules:
  - web
  - reports
---

# Summary

Phase 4 **S4 — reports**. New `features/reports/`: a controls bar (date range +
interval), eight summary metric cards, and four Recharts charts (cash flow,
balance trend, category breakdown, project cost) over `/api/v1/reports/*`. Adds
`recharts` (via shadcn `chart`) + `card`, lazy-loaded so the chart bundle ships
only on `/reports`.

# Files Touched

- `web/src/features/reports/hooks.ts` (new) — `useSummary/useCashFlow/
  useCategoryReport/useProjectReport/useBalanceTrend`; balance-trend is bounded by
  `to` only (cumulative, ignores `from`).
- `web/src/features/reports/charts.tsx` (new) — 4 presentational Recharts charts,
  decimal-string totals → numbers for plotting, "No data" empty state.
- `web/src/features/reports/ReportsPage.tsx` (new) — controls, 8 metric cards
  (savings_rate `null`→"—", runway `null`→"∞"), 4 chart cards.
- `web/src/lib/money.ts` — +`toNumber` (chart plotting only).
- `web/src/index.css` — `--chart-1..5` paper palette tokens + `@theme` mapping.
- `web/src/features/transactions/hooks.ts` — writes now also invalidate
  `["reports"]` (reports derive from transactions; wiring the new feature's
  freshness).
- `web/src/App.tsx` — `/reports` route `React.lazy` + `Suspense` (code-splits
  Recharts out of the main bundle).
- `web/src/components/ui/{chart,card}.tsx` — shadcn primitives (recharts pulled).
- `docs/wiki/findings.md` / `resolutions.md` — F-020 ↔ R-020.

# Previous Behavior

`/reports` was a placeholder. No charts, no derived figures in the UI; bundle had
no chart lib.

# New Behavior

Reports page shows balances/income/expense/net/savings-rate/burn/runway plus four
charts, all recomputed server-side and refetched when the ledger changes. Nullable
ratios render as "—"/"∞". Recharts (~363 kB) loads only when the page is opened.

# Reason For Change

Approved Phase 4 slice S4 — the read-only reporting/visualization layer.

# Decisions (user, this session)

- **Recharts via shadcn `chart`**, paper-toned `--chart-1..5`; bar charts for
  cash-flow/category/project, line for balance trend.
- **Lazy-load reports** to keep Recharts out of the initial bundle (main 921→557
  kB; reports a separate 363 kB / 107 kB gz chunk).
- **Transaction writes invalidate `["reports"]`** — touches the committed S3 hook,
  but it's the wiring that makes the new reports refresh (not unrelated cleanup).
- **F-020 wontfix** — `(--var)` is valid Tailwind v4 shorthand (proven in built
  CSS); CodeRabbit applied v3 rules to vendored code.

# Risks

- New surface only; no API/contract/migration change.
- Residual >500 kB main-chunk warning (radix + query + router) — acceptable for a
  self-hosted internal tool; further splitting deferred (diminishing returns).
- Chart values go through `Number()` (`toNumber`) — plotting only, same float
  caveat as `formatAmount`.

# Notes

- **Live smoke** vs throwaway Postgres 17 (`kanjo-s4-smoke` on 5433, `--rm`), API
  :3000, SPA Vite proxy. Empty DB → `savings_rate`/`runway_months` null,
  cash-flow `[]`. Seeded BCA(liquid)/Investments(illiquid), Food/Fees, LazyScan,
  3 entries + a transfer-with-fee; June window asserts all matched by hand:
  current_balance 7,808,500 (transfer legs cancel), liquid 7,308,500 (excl.
  Investments), income 10,000,000, expense 2,191,500, net 7,808,500, savings_rate
  0.7809, monthly_burn 2,191,500, runway 3.33; cash-flow one June bucket;
  categories Food 2,000,000 / Uncategorized 185,000 / Fees 6,500; project LazyScan
  185,000; balance-trend cumulative → 7,808,500. Container removed, ports freed.
- CodeRabbit (`-t uncommitted`): 1 critical, proven false positive (Tailwind v4
  syntax), wontfix. `pnpm build`/`pnpm lint` green under strict.
- Constraints honored: smallest safe slice; vendored primitives unpatched; no API
  touched; no deploy artifacts.
- Committed this phase (S4).

---
time: 02:50 PM
type: feature
breaking_change: false
modules:
  - web
---

# Summary

Phase 4 **S5 — dashboard** (last slice; closes the S0→S5 web plan). New
`features/dashboard/DashboardPage.tsx` replaces the `/` index placeholder with the
landing overview from the design-system IA: four metric cards (Current Balance,
Monthly Burn, Savings Rate, Runway) over `/api/v1/reports/summary` + a Recent
Transactions card (newest 8, read-only) over `/api/v1/transactions`, with a
"View all" link to the ledger. Pure reuse — no new endpoint, hook, or dependency.

# Files Touched

- `web/src/features/dashboard/DashboardPage.tsx` (new) — reuses `useSummary({})`
  (reports) and `useTransactions({})` (transactions, same cache key as the ledger's
  unfiltered view) + `useResourceList` for account/category name maps. Nullable
  `savings_rate`/`runway_months` render "—"/"∞" (mirrors ReportsPage). Compact
  transaction table copies the ledger's signed/colored amount rendering; loading/
  error/empty states handled. Local `Metric`/`RowMessage` helpers (small dup, kept
  self-contained to avoid touching the committed ReportsPage).
- `web/src/App.tsx` — `/` index now renders `<DashboardPage />` (was
  `<Placeholder title="Dashboard" />`); dropped the placeholder import + stale
  "routes render placeholders" comment.
- `web/src/components/Placeholder.tsx` (deleted) — now dead; every route has a real
  page.

# Previous Behavior

`/` rendered the generic placeholder ("Coming in a later slice."). The dashboard —
the first thing the design system specifies — did not exist; `Placeholder` was used
only by this route.

# New Behavior

The landing page shows the four headline metrics and the latest transactions,
recomputed server-side and refetched whenever the ledger changes (transaction
writes already invalidate `["reports"]` and `["transactions"]` from S3/S4). Window
figures use the report default (trailing-3-month); balance is all-time stock. No
API change.

# Reason For Change

Approved Phase 4 slice S5 — the dashboard/overview vertical, last of the sliced
web delivery.

# Decisions (this session)

- **Reuse over rebuild** — dashboard composes the already-validated `summary` +
  `transactions` endpoints and copies the proven ledger/report rendering, rather
  than adding hooks or charts. No Recharts on the dashboard → no lazy chunk needed
  (it ships in the main bundle, correct for the landing route).
- **Self-contained `Metric` card** — duplicated ~10 lines rather than exporting/
  refactoring the committed `ReportsPage` (avoid mixing a refactor into the slice).
- **Deleted `Placeholder`** — dead after this slice; removing it is in-scope (the
  index route is what this slice replaces).

# Risks

- New surface only; no API/contract/migration change, no new dependency.
- Cross-feature hook import (dashboard → reports/transactions hooks) — these are the
  shared data layer; acceptable.
- Residual >500 kB main-chunk warning unchanged (pre-existing radix+query+router);
  not introduced here.

# Notes

- `pnpm build` (tsc + vite) green; `pnpm lint` clean under strict; `git diff
  --check` clean.
- **API validation: not applicable** — no endpoint/contract/auth/business-flow
  change. The dashboard only reads `/reports/summary` (validated live in S4) and
  `/transactions` list (validated live in S3); rendering is copied from the
  already-smoke-tested LedgerPage/ReportsPage. No Bruno run required. Manual GUI
  pass left for the human gate.
- CodeRabbit (`-t uncommitted`, with AGENTS.md + Constraint.md + design-system.md
  as `-c`): **No findings**, clean first pass. Raw output archived to
  `/Users/konnco/Projects/wiki/kanjo/coderabbit/sessions/22-06-2026.md`.
- Constraints honored: smallest safe slice; no API touched; committed sibling code
  untouched; no deploy artifacts.
